# Rust is owned by rustup via THIS file -- never mise (mise would export
# RUSTUP_TOOLCHAIN and override it). `ci:check-global` enforces rust stays out
# of [tools]. stable satisfies the nu 0.113.1 MSRV (1.93.1).
[toolchain]
channel = "stable"
